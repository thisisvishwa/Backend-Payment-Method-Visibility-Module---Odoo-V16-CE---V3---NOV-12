```xml
<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <!-- Rule for Payment Method -->
        <record id="payment_method_rule" model="ir.rule">
            <field name="name">Payment Method Rule</field>
            <field name="model_id" ref="model_payment_method"/>
            <field name="groups" eval="[(4, ref('group_payment_method_user'))]"/>
            <field name="perm_read" eval="True"/>
            <field name="perm_write" eval="True"/>
            <field name="perm_create" eval="True"/>
            <field name="perm_unlink" eval="True"/>
            <field name="domain_force">['|',('user_id','=',user.id),('user_id','=',False)]</field>
        </record>

        <!-- Rule for Sale Order -->
        <record id="sale_order_rule" model="ir.rule">
            <field name="name">Sale Order Rule</field>
            <field name="model_id" ref="model_sale_order"/>
            <field name="groups" eval="[(4, ref('group_sale_order_user'))]"/>
            <field name="perm_read" eval="True"/>
            <field name="perm_write" eval="True"/>
            <field name="perm_create" eval="True"/>
            <field name="perm_unlink" eval="True"/>
            <field name="domain_force">['|',('user_id','=',user.id),('user_id','=',False)]</field>
        </record>

        <!-- Rule for Account Invoice -->
        <record id="account_invoice_rule" model="ir.rule">
            <field name="name">Account Invoice Rule</field>
            <field name="model_id" ref="model_account_invoice"/>
            <field name="groups" eval="[(4, ref('group_account_invoice_user'))]"/>
            <field name="perm_read" eval="True"/>
            <field name="perm_write" eval="True"/>
            <field name="perm_create" eval="True"/>
            <field name="perm_unlink" eval="True"/>
            <field name="domain_force">['|',('user_id','=',user.id),('user_id','=',False)]</field>
        </record>
    </data>
</odoo>
```