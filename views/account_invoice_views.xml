```xml
<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <data>
        <!-- Inherit Form View: account.invoice.form -->
        <record id="view_account_invoice_form_inherit_payment_method" model="ir.ui.view">
            <field name="name">account.invoice.form.inherit.payment.method</field>
            <field name="model">account.invoice</field>
            <field name="inherit_id" ref="account.view_move_form"/>
            <field name="arch" type="xml">
                <xpath expr="//field[@name='partner_id']" position="after">
                    <field name="payment_method_id"/>
                </xpath>
            </field>
        </record>

        <!-- Inherit Tree View: account.invoice.tree -->
        <record id="view_account_invoice_tree_inherit_payment_method" model="ir.ui.view">
            <field name="name">account.invoice.tree.inherit.payment.method</field>
            <field name="model">account.invoice</field>
            <field name="inherit_id" ref="account.view_invoice_tree"/>
            <field name="arch" type="xml">
                <xpath expr="//field[@name='partner_id']" position="after">
                    <field name="payment_method_id"/>
                </xpath>
            </field>
        </record>

        <!-- Inherit Search View: account.invoice.search -->
        <record id="view_account_invoice_search_inherit_payment_method" model="ir.ui.view">
            <field name="name">account.invoice.search.inherit.payment.method</field>
            <field name="model">account.invoice</field>
            <field name="inherit_id" ref="account.view_account_invoice_filter"/>
            <field name="arch" type="xml">
                <xpath expr="//filter[@name='unpaid']" position="after">
                    <filter string="Payment Method" name="groupby_payment_method" domain="[]" context="{'group_by':'payment_method_id'}"/>
                </xpath>
            </field>
        </record>
    </data>
</odoo>
```