```xml
<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <!-- Automated Action for Real-time Updates -->
        <record id="action_payment_method_update" model="ir.actions.server">
            <field name="name">Update Payment Method</field>
            <field name="model_id" ref="model_payment_method"/>
            <field name="state">code</field>
            <field name="code">
                record.update_payment_method()
            </field>
        </record>

        <!-- Automated Action for Notification -->
        <record id="action_payment_method_notify" model="ir.actions.server">
            <field name="name">Notify Payment Method Change</field>
            <field name="model_id" ref="model_payment_method"/>
            <field name="state">code</field>
            <field name="code">
                record.notify_payment_method_change()
            </field>
        </record>
    </data>
</odoo>
```