```xml
<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <!-- Email template for Payment Method Update Notification -->
        <record id="email_template_payment_method_update" model="mail.template">
            <field name="name">Payment Method Update Notification</field>
            <field name="model_id" ref="model_payment_method"/>
            <field name="subject">Payment Method Update: ${object.name}</field>
            <field name="email_from">${(user.email_formatted) | safe}</field>
            <field name="email_to">${(object.user_id.email_formatted) | safe}</field>
            <field name="body_html" type="html">
                <div>
                    <p>Hello ${object.user_id.name},</p>
                    <p>The payment method ${object.name} has been updated. Please check the details in your Odoo backend.</p>
                    <p>Thank you,</p>
                    <p>${user.name}</p>
                </div>
            </field>
        </record>

        <!-- Email template for New Payment Method Notification -->
        <record id="email_template_new_payment_method" model="mail.template">
            <field name="name">New Payment Method Notification</field>
            <field name="model_id" ref="model_payment_method"/>
            <field name="subject">New Payment Method: ${object.name}</field>
            <field name="email_from">${(user.email_formatted) | safe}</field>
            <field name="email_to">${(object.user_id.email_formatted) | safe}</field>
            <field name="body_html" type="html">
                <div>
                    <p>Hello ${object.user_id.name},</p>
                    <p>A new payment method ${object.name} has been added. Please check the details in your Odoo backend.</p>
                    <p>Thank you,</p>
                    <p>${user.name}</p>
                </div>
            </field>
        </record>
    </data>
</odoo>
```